[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "onnxforge-shared"
version = "0.1.0"
description = "Shared schemas and utilities for ONNXForge"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]